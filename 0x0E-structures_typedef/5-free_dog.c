#include "dog.h"
#include <stdio.h>
#include <stdlib.h>

/**
  * free_dog - Free Dog
  * @d:  dog
  *
  */

void free_dog(dog_t *d)
{
	dog_t *d = malloc(sizeof(dog_t));

	if (dot_t == NULL)
	{
		free(dog_t);

		return (NULL);
	}
}
