#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * strtow - str
 * @str: str
 * Return: Return
 */

char **strtow(char *str)
{
	if (str == NULL || str == " ")
	{
		return (NULL);
	}

	return (0);
}
