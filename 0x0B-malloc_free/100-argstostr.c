#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * argstostr - argstostr
 * @ac: ac
 * @av: av
 * Return: Return
 */

char *argstostr(int ac, char **av)
{
	if (ac == 0 || av == NULL)
	{
		return (NULL);
	}

	return (0);
}
