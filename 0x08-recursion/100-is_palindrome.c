#include "main.h"

/**
 * is_palindrome - Palindrome
 * @s: s
 * Return: Palindrome
 */

int is_palindrome(char *s)
{
	if (*s != '\0')
	{
		temp[] = *s;

		if (*s == temp)
		{
			return (1);
		}

		return (0);
	}
}
