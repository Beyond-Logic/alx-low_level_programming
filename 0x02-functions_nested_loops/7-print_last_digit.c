#include "main.h"
#include <stdlib.h>

/**
 * print_last_digit - print last digit
 * @n: n to be checked
 *
 * Return: the value of the last digit
 */

int print_last_digit(int n)
{
	int i;

	for (i = 1; i <= 2; i++)
	{
		return (abs(n) % 10);
	}
	return (0);
}
