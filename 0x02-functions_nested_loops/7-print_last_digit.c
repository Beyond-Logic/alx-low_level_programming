#include "main.h"
#include <stdlib.h>

/**
 * print_last_digit - print last digit
 * @n: n to be checked
 *
 * Return: the value of the last digit
 */

int print_last_digit(int n)
{
	return (abs(n) % 10);
}
